{
  "name": "local-storage",
  "private": true,
  "workspaces": [
    "api",
    "frontend-local-storage"
  ],
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --workspace=frontend-local-storage",
    "start": "npm run start --workspace=api",
    "dev": "npm run build && npm run dev --workspace=api",
    "dev:app": "npm run start --workspace=frontend-local-storage",
    "dev:api": "npm run dev --workspace=api"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.10.0"
  },
  "dependencies": {
    "axios": "^0.26.0",
    "cross-env": "^7.0.3",
    "react-router-dom": "^6.2.2"
  }
}
